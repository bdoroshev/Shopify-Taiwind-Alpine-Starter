{% case media.media_type %}
  {% when 'image' %}
    {{ media | image_url: width: 600 | image_tag: loading: 'lazy', class: class }}
  {% when 'video' %}
    {{ media | video_tag: controls: true, class: class }}
  {% when 'external_video' %}
    {{ media | external_video_tag: class: 'aspect-video w-full' }}
  {% when 'model' %}
    {{ media | model_viewer_tag: reveal: 'interaction', class: 'w-full min-h-[400px] md:min-h-[200px]' }}
  {% else %}
    {{ media | media_tag }}
{% endcase %}
